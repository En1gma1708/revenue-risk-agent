fastapi==0.141.1
uvicorn==0.46.0
pydantic==2.13.4
python-dotenv==1.2.2
google-genai==1.75.0
groq==0.37.1
openai==3.3.1
razorpay==2.0.1
openpyxl==3.1.5
python-multipart==0.0.32
# google-genai extra deliberately excluded: pydantic-ai-slim 2.36.0 requires google-genai>=2.18.0,
# which conflicts with the google-genai==1.75.0 pin above (llm_client.GeminiClient's dependency).
# Not a real loss right now -- Gemini is already deprioritized (see CLAUDE.md) and unused by the
# pydantic_agents.py PoC's default provider. Add google-genai>=2.18.0 + [google] here if Gemini
# support in the Pydantic AI system is ever actually needed.
pydantic-ai-slim[groq,openai]==2.36.0

# Langfuse -- observability/eval for pydantic_agents.py's router/specialist/checker agents.
# Consumes PydanticAI's native OTel instrumentation (Agent.instrument_all()), no framework
# lock-in. Bumps opentelemetry-* to 1.44.0, which conflicts with chromadb's pinned
# opentelemetry-exporter-otlp-proto-grpc==1.41.1 requirement -- confirmed harmless, chromadb is
# not a dependency of this project (not in this file, never imported here); the conflict is only
# between two unrelated packages that happen to share this environment.
langfuse==4.15.1
